// Interfaces and types for the {{pascalCase name}} model
import { Document } from "mongoose";

import { I{{ pascalCase name }} } from "../../types/entity.types";

// Model interface
export interface I{{ pascalCase name }}Model extends I{{ pascalCase name }},Document {}
